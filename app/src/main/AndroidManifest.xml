<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lying.myword">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StartUpActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".WordsModuleListActivity" />
        <activity android:name=".ModuleWordsReclerViewActivity" />
        <activity android:name=".StudyActivity" />
        <activity android:name=".ReviewActivity" />
        <activity android:name=".StudySurveyActivity" />
        <activity android:name=".StudySurveyOfSpecificActivity" />
        <activity
            android:name=".TestWordsActivity"
            android:windowSoftInputMode="adjustResize"></activity>
        <activity android:name=".TestResultActivity"></activity>
    </application>

</manifest>